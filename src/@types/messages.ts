export type SendMessageProps = {
  to: string;
  body: string;
};

export type SendMessageLinkProps = {
  to: string;
  body: string;
  url: string;
};

export type SendFileLinkProps = {
  to: string;
};

export type SendFileProps = {
  to: string;
};
